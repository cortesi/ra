[package]
name = "ra-document"
version = "0.1.0"
edition = "2024"
description = "Document parsing and chunking for ra"

[lints]
workspace = true

[dependencies]
pulldown-cmark = "0.13"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
